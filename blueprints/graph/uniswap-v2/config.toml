[store]
[store.primary]
connection = "postgresql://graph-node:let-me-in@postgres:5432/graph-node"
pool_size = 10

[deployment]
[[deployment.rule]]
store = "primary"
indexers = ["default"]

[chains]
ingestor = "default"

[chains.mainnet]
shard = "primary"
provider = [
  { label = "erpc", url = "http://erpc-latest-only-network-level-failsafe:4000/main/evm/1", features = ["archive"] }
]

[chains.arbitrum-one]
shard = "primary"
provider = [
  { label = "erpc", url = "http://erpc-latest-only-network-level-failsafe:4000/main/evm/42161", features = ["archive"] }
]

[chains.base]
shard = "primary"
provider = [
  { label = "erpc", url = "http://erpc-latest-only-network-level-failsafe:4000/main/evm/8453", features = ["archive"] }
]